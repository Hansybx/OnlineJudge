language: python
python:
    - "3.6"
services:
    - docker
install:
    - pip install -r deploy/requirements.txt
    - echo `cat /dev/urandom | head -1 | md5sum | head -c 32` > data/config/secret.key
    - ./init_db.sh
script:
    - docker ps -a
    - docker login --username=hansybx registry.cn-hangzhou.aliyuncs.com
    - docker build -t oj-backend:latest .
    - docker images
    - docker push oj-backend:latest
    - echo "aaa"
notifications:
    slack: onlinejudgeteam:BzBz8UFgmS5crpiblof17K2W